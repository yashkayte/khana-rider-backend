version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: khana_rider_mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: khana_rider
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
